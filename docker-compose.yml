services:
  postgres_doc:
    image: ankane/pgvector:latest
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: document_chat
    ports:
      - "5433:5432"
    volumes:
      - pgdata_doc:/var/lib/postgresql/data

volumes:
  pgdata_doc: